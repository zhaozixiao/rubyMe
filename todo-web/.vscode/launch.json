{
    "configurations": [
        {
            "command": "yarn run start",
            "name": "Run yarn start",
            "request": "launch",
            "type": "node-terminal"
        }
    
    ]
}